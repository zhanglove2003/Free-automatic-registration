{
  "name": "register-orchestrator-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Windows desktop automation skeleton with safe integration boundaries.",
  "type": "module",
  "main": "dist/src/main/main.js",
  "scripts": {
    "test": "vitest run",
    "build": "tsc -p tsconfig.json && node scripts/copy-assets.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "npm run build && electron ."
  },
  "dependencies": {
    "electron-store": "^10.0.1",
    "zod": "^3.25.67"
  },
  "devDependencies": {
    "@types/node": "^24.13.2",
    "electron": "^42.5.1",
    "typescript": "^5.8.3",
    "vitest": "^4.1.9"
  }
}
